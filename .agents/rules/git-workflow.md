# Git Commit and Push Policy

- Do NOT automatically commit changes using `git commit`.
- Do NOT automatically push changes using `git push`.
- Leave all git staging, committing, and pushing for the user to perform manually, unless the user explicitly requests a commit or push.
